#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

cd "$ROOT_DIR"

nvim --headless --clean --noplugin -i NONE -u "$ROOT_DIR/tests/minimal_init.lua" -c "lua require('tests.run').run()"
